import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF0077FF); // Rizzume Blue
  static const Color secondary = Color(0xFF00C6AE);
  static const Color accent = Color(0xFF6C63FF);

  // Floating deck & neon accents
  static const Color deckBackground = Color(0xFF1F1E1E);
  static const Color deckBorder = Color(0xFF2B9CFF);
  static const Color neonRed = Color(0xFFFF2D55);
  static const Color neonBlue = Color(0xFF00E5FF);

  // Background & surfaces
  static const Color background = Color(0xFFF8FAFD);
  static const Color card = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE0E0E0);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6F6F6F);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFE53935);

  // Chat & misc
  static const Color messageSent = Color(0xFFDCF8C6);
  static const Color messageReceived = Color(0xFFFFFFFF);

  // Dark mode (optional)
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkCard = Color(0xFF1E1E1E);
}